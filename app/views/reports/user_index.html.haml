= content_for :title do
  Reports - #{@user.name}

.row
  .col-md-12
    %h1
      = gravatar_for @user
      #{@user.name}'s reports
.row
  .col-md-12
    %ul
      - @reports.each do |report|
        %li
          = link_to report.today, report_path(report)

