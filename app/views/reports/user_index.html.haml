= content_for :title do
  Reports - #{@user.name}

.row
  .col-md-12
    %h1
      = gravatar_for @user
      #{@user.name}'s reports
.row
  .col-md-12
    %ul#reports-index
      - @reports.each do |report|
        %li
          = link_to report.today.to_s(:display), report_path(report)

